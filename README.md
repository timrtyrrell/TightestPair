# TightestPair
Algorithm that computes the closest pair of Cartesian points in a plane.
-----
Task: develop an algorithm that takes as an input a set of Cartesian coordinates and computes the closest pair in O(n*log(n)) runtime
